# =============================================================================
# 1) MQTT (HiveMQ Cloud / Broker อื่นก็ได้)
#    - ใส่โฮสต์/พอร์ต/บัญชี ที่ได้รับจากผู้ให้บริการ
#    - ถ้าใช้ TLS ให้ตั้ง MQTT_TLS=1 และพอร์ตทั่วไปคือ 8883
#    - ถ้าใช้ WebSocket (wss) ให้ตั้ง MQTT_WS=1 และมักใช้พอร์ต 443
# =============================================================================
MQTT_HOST=<CHANGE_ME>.hivemq.cloud
MQTT_PORT=8883
MQTT_USERNAME=<CHANGE_ME>
MQTT_PASSWORD=<CHANGE_ME>
MQTT_TLS=1          # 1=เปิด TLS, 0=ปิด (ถ้า 0 มักใช้พอร์ต 1883)
MQTT_WS=0           # 1=ใช้ MQTT over WebSocket (wss), 0=TCP ปกติ
MQTT_CLIENT_ID=C01-rpi4    # ต้องไม่ซ้ำกันในระบบ (เช่น C01-rpi4, C02-rpi4-02)

# (ทางเลือก) CA ของ Broker (ไฟล์ .pem)
# ถ้าไม่ตั้งค่า ระบบจะใช้ CA ของ OS อัตโนมัติ
# ตัวอย่างบน Debian/Ubuntu:
# MQTT_CA=/etc/ssl/certs/ca-certificates.crt
# MQTT_CA=/path/to/hivemq-cloud-chain.pem

# =============================================================================
# 2) อัตลักษณ์อุปกรณ์ / โครงสร้างตู้
#    - CUPBOARD_ID: รหัสตู้นี้ (เช่น C01)
#    - SLOT_IDS:    รายการช่องในตู้ เรียงตามการเดินสายจริง (คั่นด้วย comma)
# =============================================================================
CUPBOARD_ID=C01
SLOT_IDS=SC001,SC002,SC003,SC004

# =============================================================================
# 3) ค่าพฤติกรรมระบบ/รอบการทำงาน
#    - CHECK_INTERVAL: วินาทีต่อรอบการตรวจ/ส่งสถานะ
#    - LOG_LEVEL:      DEBUG/INFO/WARNING/ERROR (แนะนำ INFO ปกติ)
# =============================================================================
CHECK_INTERVAL=120
LOG_LEVEL=INFO
# LOG_LEVEL=DEBUG

# =============================================================================
# 4) เกณฑ์ตีความค่าเซนเซอร์ (ปรับตามการติดตั้งจริง)
#    - ZERO_THRESHOLD: ระยะ (มม.) ที่ถือว่า "ชิดเซนเซอร์" หรือ "เต็ม/0%"
#      ตัวอย่าง: ถ้าน้อยกว่าหรือเท่ากับค่าเกณฑ์นี้ ให้ถือว่าใกล้มาก (ส่งค่า -1 ในโค้ดอ่านเซนเซอร์)
# =============================================================================
ZERO_THRESHOLD=70
